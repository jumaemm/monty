#include "monty.h"

/**
 * _nop - does nothing, nada
 * @stack: pointer to stack
 * @line_count: line counter
 * Return: Always 0
 */
void _nop(stack_t **stack, unsigned int line_count)
{
	(void) line_count;
	(void) stack;
}
